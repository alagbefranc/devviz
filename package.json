{
  "name": "devviz-platform",
  "version": "1.0.0",
  "description": "AI-Powered Development Visualization Platform",
  "scripts": {
    "build": "node build.js",
    "start": "node server.js",
    "deploy": "npm run build && netlify deploy --prod --site=devviz-platform"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@supabase/supabase-js": "^2.45.7"
  },
  "devDependencies": {
    "netlify-cli": "^23.7.3"
  }
}